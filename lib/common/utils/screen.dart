import 'package:get/get.dart';

class RsScreen {
  static double w = Get.width;
  static double h = Get.height;
  static double w1 = w / 100;
  static double h1 = h / 100;
  static double wn(n) => w / n;
  static double hn(n) => h / n;
}
